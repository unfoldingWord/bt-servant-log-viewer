# BT Servant Log Viewer - Environment Configuration
# Copy this file to .env and fill in the values

# ============================================================================
# BT-Servant API Configuration (9 environment variables required)
# ============================================================================
# Each environment needs URL, TOKEN (for /admin/logs/*), and ALIVE_TOKEN (for /alive):
# - URL: Base URL for the BT-Servant instance (configurable, has defaults)
# - TOKEN: Bearer token for /admin/logs/* endpoints (required, no default)
# - ALIVE_TOKEN: Bearer token for /alive health check endpoint (required, no default)

# Development Environment (typically localhost)
BT_SERVANT_DEV_URL=http://localhost:8080
BT_SERVANT_DEV_TOKEN=your-dev-admin-logs-token-here
BT_SERVANT_DEV_ALIVE_TOKEN=your-dev-alive-token-here

# QA Environment
BT_SERVANT_QA_URL=https://qa.servant.bible
BT_SERVANT_QA_TOKEN=your-qa-admin-logs-token-here
BT_SERVANT_QA_ALIVE_TOKEN=your-qa-alive-token-here

# Production Environment
BT_SERVANT_PROD_URL=https://app.servant.bible
BT_SERVANT_PROD_TOKEN=your-prod-admin-logs-token-here
BT_SERVANT_PROD_ALIVE_TOKEN=your-prod-alive-token-here

# ============================================================================
# Notes:
# - URLs are optional (defaults shown above) but both tokens are required
# - Servers without valid tokens won't appear in the UI dropdown
# - The health check calls /alive with ALIVE_TOKEN every 60 seconds
# - Log fetching calls /admin/logs/* with TOKEN
# ============================================================================
