# BT Servant Log Viewer API - Environment Configuration
# Copy this file to .env and fill in the values

# Server Configuration
NODE_ENV=development
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info

# CORS Configuration
# In development, set to true to allow all origins
# In production, set to your frontend URL (e.g., https://bt-log-viewer.fly.dev)
CORS_ORIGIN=true

# ============================================================================
# BT-Servant API Configuration (6 environment variables required)
# ============================================================================
# Each environment needs both a URL and a TOKEN:
# - URL: Base URL for the BT-Servant instance (configurable, has defaults)
# - TOKEN: Bearer token for authenticating with the API (required, no default)

# Development Environment (typically localhost)
BT_SERVANT_DEV_URL=http://localhost:8080
BT_SERVANT_DEV_TOKEN=your-dev-token-here

# QA Environment
BT_SERVANT_QA_URL=https://qa.servant.bible
BT_SERVANT_QA_TOKEN=your-qa-token-here

# Production Environment
BT_SERVANT_PROD_URL=https://app.servant.bible
BT_SERVANT_PROD_TOKEN=your-prod-token-here

# ============================================================================
# Notes:
# - URLs are optional (defaults shown above) but tokens are required
# - Servers without valid tokens won't appear in the UI dropdown
# - The health check endpoint tests each server on app startup
# ============================================================================
